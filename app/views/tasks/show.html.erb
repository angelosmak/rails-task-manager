<div class="card px-2 py-2 mt-3 mx-5">
  <div class="d-flex justify-content-between">
    <h2><%= @task.title %></h2>
    <a class="btn btn-primary p-2" href=<%= task_edit_path(@task) %>>Edit task</a>
  </div>
  <div class="card mx-6">
    <p><%= @task.details %></p>
  </div>
<%= link_to "Back to tasks!", tasks_path%>
</div>
